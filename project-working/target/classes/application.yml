spring:
  batch:
    job:
      enabled: false
  datasource:
    url: jdbc:h2:mem:batchdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

---

spring:
  config:
    activate:
      on-profile: local

app:
  local-path: C:/data/input/local-file.csv
  bucket-name: your-local-bucket
  object-key: archive/local-file.csv
  event-time: 2025-03-16T10:00:00Z
